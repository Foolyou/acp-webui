name: acp-webui-transcription

services:
  faster-whisper-server:
    image: fedirz/faster-whisper-server:latest-cuda
    ports:
      - "127.0.0.1:7322:8000"
    volumes:
      - whisper-cache:/root/.cache/huggingface
    gpus: all
    restart: unless-stopped

volumes:
  whisper-cache:
