## 1. Frontend Flow

- [ ] 1.1 Update the New Session orchestration to call session creation without prompt content, then submit the compose prompt to the created session through the prompt API.
- [ ] 1.2 Preserve last-profile storage, creating-session indicators, route navigation, session list updates, and prompt error handling for the two-step flow.

## 2. Tests

- [ ] 2.1 Update frontend API and route/unit tests so New Session compose verifies empty session creation before prompt dispatch.
- [ ] 2.2 Run focused frontend tests for API behavior, route compose behavior, and app session creation orchestration.
