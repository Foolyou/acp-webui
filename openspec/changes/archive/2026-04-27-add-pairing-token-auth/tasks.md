## 1. Backend Auth Foundation

- [x] 1.1 Add backend configuration for pairing token, auth disablement for development, and trusted client IP/CIDR entries.
- [x] 1.2 Implement an auth module for token generation, token verification, opaque browser session creation, cookie construction, and auth state classification.
- [x] 1.3 Implement trusted client parsing and matching for direct peer IP addresses, including loopback defaults and explicit CIDR entries.
- [x] 1.4 Ensure forwarded headers such as `X-Forwarded-For` and `Forwarded` are ignored by trusted client matching.
- [x] 1.5 Add process-local failed pairing attempt throttling or backoff keyed by client IP.

## 2. Backend Routes and Protection

- [x] 2.1 Add public `GET /api/auth/status` and `POST /api/auth/pair` endpoints.
- [x] 2.2 Protect existing sensitive HTTP API routes so unpaired untrusted requests return unauthorized before handlers mutate or disclose state.
- [x] 2.3 Protect `/api/ws` so unpaired untrusted browsers cannot receive realtime events.
- [x] 2.4 Include auth state in responses where useful for frontend initialization without returning the active pairing token.
- [x] 2.5 Print safe startup pairing guidance, including bind address and generated token when the token is generated by the daemon.

## 3. Frontend Pairing Flow

- [x] 3.1 Add frontend auth API types and client methods for status and pairing.
- [x] 3.2 Add app initialization behavior that checks auth status before loading protected app state.
- [x] 3.3 Build a mobile-friendly pairing view with token input, submit behavior, loading state, and readable error display.
- [x] 3.4 Route successful pairing into the existing app state loading and navigation restore flow.
- [x] 3.5 Handle unauthorized API responses during app use by returning to the pairing flow and clearing sensitive current UI state.
- [x] 3.6 Ensure WebSocket connection is only opened after authenticated or trusted access is established.

## 4. Tests

- [x] 4.1 Add backend tests for anonymous rejection on protected API endpoints.
- [x] 4.2 Add backend tests for successful pairing, invalid token rejection, and session cookie access.
- [x] 4.3 Add backend tests for trusted loopback access, configured trusted CIDR access, and untrusted private LAN access.
- [x] 4.4 Add backend tests proving forwarded headers do not bypass pairing.
- [x] 4.5 Add backend tests for WebSocket rejection and acceptance across anonymous, paired, and trusted access modes.
- [x] 4.6 Add frontend or Playwright coverage for anonymous app load, successful pairing, failed pairing, and post-pairing app restore.

## 5. Documentation and Validation

- [x] 5.1 Update README run instructions with pairing token behavior, trusted client configuration, and safe bind-host examples.
- [x] 5.2 Update product design notes if implementation resolves token format, logout, or startup logging open questions.
- [x] 5.3 Run OpenSpec validation for `add-pairing-token-auth`.
- [x] 5.4 Run backend tests, frontend lint/build, and relevant Playwright checks.
